# 团队界面视觉验收 · 2026-09-09

最终结果：passed（本次团队界面修复范围）。这不是整套业务流程、全站或移动端全面验收结论。

## 依据与范围

- 已确认原稿：`docs/design/references/{review,questions,feedback,progress}.png`。
- 规范：`docs/design/team-ui.md`；后续会话入口：根目录 `AGENTS.md`；跨项目约束：`~/.codex/AGENTS.md` 第 9 节。
- 实现：`packages/web/src/ui/`、`task-resource-panel.ts`、`task-review-card.ts`、`team-question-card.ts`、`team-progress-card.ts`、`a2ui/renderer.ts`、`review-diff.ts`。
- 实际宿主：DSH rc.6，http://127.0.0.1:3080/，Chrome 插件。主对照视口 1586×992；另查 1024×768、768×900。

## 发现与复验

| 问题 | 原因与处理 | 结果 |
| --- | --- | --- |
| 资源栏窄、控件粗重，未还原稿件 | 未提取布局比例，局部组件替换被误当整体完成；统一局部主题、组件、图标及 36% 资源栏 | 主视口实际资源宽 570px，原稿约 571px |
| 聊天卡片被截断 | 局部 box-sizing 漏掉根节点，100% 宽度又叠加 padding | border-box；卡片完整位于聊天区域 |
| 按钮字体不一致 | 无效 font 简写经优化后回退浏览器默认字号 | 最终真实页面计算字号 14px |
| 修改内容层级不清 | 原始差异块过重 | 按真实 Markdown 章节列出修改，可展开修改前内容；补相关文档入口 |
| 小窗口资源打开后仍隐藏 | DSH 自动折叠详情，插件显示状态与宿主布局不一致 | 仅在资源组件挂载时适配布局；1024px 并列，768px 上下分区；关闭时清理 |
| 窄窗口阅读和审批互相挤压 | 固定底栏与不足的阅读高度 | 紧凑模式资源整体滚动；输入框和审批操作可达 |

## 实际检查

- 构建、工作区类型检查通过；相关 22 个测试文件、119 项测试通过；变更源文件及新增验收载体 ESLint 通过；diff 空白检查通过。
- 使用真实 DSH 的 React、Markdown、布局与插槽，在单个 Chrome 标签页替换测试客户端，覆盖审批、逐题确认、意见回执、执行、空状态和加载失败。测试载体明确显示“UI 验收 · 模拟任务”，其团队请求及等待响应均为本地样例，不代用户审批真实业务。
- 验证页签方向键切换、意见非空时禁止直接批准、提交失败保留草稿、关闭重开、逐题选择与稍后继续、模拟暂停、空状态及失败重试入口。
- 原稿与截图并排检查了审批、逐题确认、反馈及执行四种状态；完成两轮修正。主布局、局部控件与状态区未发现剩余 P0/P1/P2 视觉问题。
- 最终插件包已升级进本地 web profile；清除当前验收页拦截后重新加载真实页面，资源空状态正确，控制台无错误，字体与宽度符合测量值。浏览器视口已恢复。

## 证据与有意差异

本机截图位于 `.backend-team/artifacts/ui-fidelity/`：`host-review.png`、`host-questions.png`、`host-feedback.png`、`host-progress.png`、`host-narrow.png`、`host-error.png`、`actual-empty-final.png`；`compare-*.png` 为原稿左、实际右的同尺寸对照。构建、测试、安装日志在 `.backend-team/artifacts/ui-fidelity-*.log`。这些本机证据可能含现有会话内容，不纳入发布包。

保留真实宿主的聊天、侧栏和输入框，不伪造原稿示例对话。版本使用实际状态 revision，不硬编码 v2/v3。反馈回执仅表示已提交，不在缺少后端证据时显示“正在生成新版本”。实际章节内容长度不同，因此变化列表可以滚动，相关文档仍可到达。

本轮没有重新批准客户需求或执行真实业务开发；旧任务占用工作区是独立业务问题，不能据此声称已修复。深色主题和手机全流程未完成浏览器验收。测试载体不随生产插件发布。

## 2026-09-09 后续：历史审批与执行状态

本次局部修正 passed：保留原进度卡与并列资源布局，审批行不再从 BUILD 阶段推断“已确认”，改为有记录时显示“历史审批记录”，缺记录显示“审批待核实”；卡片、资源区明确所属原任务及不覆盖新增需求。idle/缺少执行记录显示“尚未运行”，wait 返回需要恢复或处理旧任务，避免无工作时轮询。

真实 Chrome 页面复验：当前客户跟进会话展示原客户管理任务名称、两条历史审批记录、功能开发“尚未运行”；资源区有历史记录说明。未操作真实批准。截图 `.backend-team/artifacts/approval-origin-live.png`（1920×906）已打开检查，主要布局保持，无控件截断；本次为状态与文案修正，未声称与原稿示例业务状态相同。22 文件 123 项测试通过，构建、类型检查、相关 ESLint 通过；本地插件升级并重启，页面无控制台错误。

审计限制：09:19 需求在原会话有 approval/asked 与 allowed-once 事件；09:56 设计只有已核实的后端 approval-recorded，尚缺对应用户操作来源，不据此宣称用户点击确认。未删除或伪造任何审批记录。旧业务开发输出目录错误仍是独立未修项，不能用本轮 UI 修复宣称完整开发链路可用。

## 2026-09-09 任务中心

本轮任务中心交互已在原型确认后实施。入口为页头“团队任务”；列表包含本工作区所有会话任务，支持状态筛选和搜索，点开查看范围、真实运行状态、产物及管理记录。文档使用现有预览；查看其他任务不会显示当前任务的审批操作。接回/搁置采用后台一次性、会话绑定、5 分钟有效的确认编号，补充意见单独保存，不同时改变任务归属。

Chrome 实装检查：使用没有绑定团队任务的额度验收会话，列出真实客户管理任务与已搁置 greeting；验证筛选、搜索空状态、详情、打开搁置确认后取消、跨会话需求文档。真实任务未被接回、搁置或批准；写操作与防重放在隔离后端测试中验证。820px 宽视口测得 document.scrollWidth=innerWidth，资源区下移，聊天输入仍可用。没有据此宣称手机全流程或深色全流程均已通过。

截图：`.backend-team/artifacts/task-center-prototype/` 中的 `list.png`、`confirmation.png` 为原型；`live-list.png`、`live-confirmation.png`、`live-document.png`、`live-narrow.png` 为真实宿主。`reference-list-1586.png` 和 `live-list-1586.png` 用 1586×992 视口复核列表布局。已打开原稿和实际截图检查，无遮挡聊天列表或输入框。组件共享同一主题；DSH 页头绑定信息放在公开 actions 插槽内，原型的独立绑定行未改造宿主页头结构。实际任务名称和文档数量来自业务记录，未伪造原型短标题。

本轮范围的交互与桌面/窄屏布局通过；整体产品完成状态仍以原需求核查清单为准。任务中心不是自动解决运行中冲突、开发中变更审批或完整交付的替代实现。

最终检查：7 个相关测试文件 44 项通过；工作区构建、类型检查、相关 ESLint 和 diff 空白检查通过。最新本地 bundle 已升级并重启。日志前缀 `.backend-team/artifacts/task-center-`。模型输出额度另按 docs/model-api.md 验证。

## 2026-09-09 开发中变更入口

复用既有逐题问题组件，新增“修改当前任务 / 另开新任务 / 暂不变更”选项及具体变更范围说明，没有新增布局或居中弹窗。取消、自由文本、旧版本回答不触发任务变更。后端与工具测试覆盖分支及中断恢复，49 项通过。

本地更新后通过 Codex Chrome 插件刷新正式页面，确认页头团队任务入口、旧任务实际绑定及尚未运行状态。此项只认证正式页面加载和原任务保持原状；未通过真实模型触发并批准业务变更，不宣称新增确认框及完整变更交付链已完成 GUI 验收。

## 2026-09-11 任务进展首页

本轮依据已确认原稿 `docs/design/references/task-progress-home-interaction-v0.1.png`（1672×941）完成任务进展首页，并把新 Bundle 升级到当前真实运行的工作区 web Profile：`.backend-team/runtime/dsh-home/profiles/web`。该 Profile 依赖的 Bundle 归档 SHA-256 为 `f3ddb94c905a618cd2f1d01a14a9509dae3c2fdb4ba6b8c9ae6bbddb7b228d2f`；重启本地 DSH 后 Chrome 真实宿主已加载新页面。

### 真实宿主检查

- 使用 Codex Chrome 插件标签页 `http://127.0.0.1:3080/`，真实会话为“人员管理功能开发”，未发送消息、未批准审批、未执行迁移或写入业务任务。
- 1672×941：标题/状态、当前进展四行、需求分析→交付五阶段、最近活动均可见；`scrollWidth === innerWidth`。
- 1024×768：无资源和有资源两种状态均可见；首次发现页头标题被操作按钮挤压成逐字换行，已将页头堆叠断点前移到 1100px，重建并复验通过。
- 820×900：资源区自动切换为上下分区，`bodyScrollWidth === innerWidth`，聊天输入仍可见。
- 390×844：侧栏折叠、资源区上下排列、输入区可达，`bodyScrollWidth === innerWidth`；页面内容需继续滚动查看完整进展。
- 深色：在宿主深色属性下复验局部主题 token、卡片、阶段线和按钮，颜色层级可读。
- 点击“查看技术详情”会展开/收起技术详情；点击“查看任务资源”打开右侧资源审阅，点击“关闭资源”恢复任务页；当前真实任务状态为“进行中”，按钮显示“恢复任务”符合其运行空闲状态。
- 重启后短暂出现未连接空状态，随后选中会话恢复为已关联任务；该过程未改动任务数据。
- Chrome 控制台仅记录重启期间的 `[web-runtime] connection lost` 重连警告；未发现页面 JavaScript 错误。
- 为覆盖真实任务当前不存在的状态，在同一个真实 DSH 宿主页面临时注入只读状态响应，未触发任何写接口；隔离样例明确使用“隔离验收”文案。已验证并保存“等待系统验证”（`home-isolated-verifying-1672x941.jpg`）、“等待你处理”（`home-isolated-pending-1672x941.jpg`）、“已受阻/已暂停”（`home-isolated-paused-blocked-1672x941.jpg`）和“已完成”（`home-isolated-delivered-1672x941.jpg`），随后清理覆盖并恢复真实任务。

截图证据保存在 `.backend-team/artifacts/task-progress-home/`：`home-1672x941-top.jpg`、`home-1024x768-fixed.jpg`、`home-1024x768-resources-fixed.jpg`、`home-820x900-resources.jpg`、`home-820x900-dark.jpg`、`home-390x844-closed.jpg`、`home-390x844-resources.jpg`、`home-technical-expanded-1920x907.jpg`、`home-resources-1920x907.jpg`。

### 差异与边界

- 页面结构、状态层级、阶段连接线、活动时间线及资源并列关系与原稿一致；DSH 自有顶栏、侧栏和底部输入区仍由宿主管理。
- 真实任务当前没有待审批、已暂停、已交付或失败状态；上述状态截图是同一真实宿主内的只读隔离覆盖，不代表业务任务已经进入这些状态。真实业务审批、迁移和交付仍未执行。
- 390px 截图中宿主顶栏按钮会因 DSH 自身窄屏策略压缩；团队首页本身无横向溢出。完整手机业务流程和真实审批仍不在本轮范围。

## 2026-09-09 变更确认实装复验与基线恢复

真实 Chrome + Qwen 会话触发 refine 的取消分支，发现两项之前单元检查未覆盖的问题：需求变更 header 未匹配团队问题组件，回退到 DSH 默认 UI；改用统一卡片后，展开的进度卡片使 composer 总高度超过宿主窗口，问题顶部被裁切。已补生产元数据选择测试、显示变更 detail、进度默认折叠，待问题确认时隐藏输入区进度（可从团队进度页签查看），问题卡片内部滚动并限制高度。AGENTS.md 增加生产 id/header/payload 与真实选择器联测约束。

已打开原 questions.png 与实际截图对照；保留 DSH 页头、侧栏、输入框，统一局部组件。最终 1586×992 Chrome 实测卡片 top=330、bottom=845.83，横向无溢出，进度未挤占确认区。内容较长时操作区通过卡片内部滚动查看，实际完成选择“暂不变更”和“提交回答”。截图 .backend-team/artifacts/change-confirmation-fixed.png；初次失败布局截图 change-confirmation-live.png 保留用于回归。未据此宣称窄屏/深色全流程通过或完全像素一致。

三次测试均明确标记仅验收取消，不执行业务变更。最终 change-gui-after.json 显示真实任务状态文件与测试前 SHA256 相同，仍 BUILD/revision 6，未替用户批准、搁置或恢复业务。新旧基线/报告隔离、真实生产审批服务的两道重审与旧确认拒绝、恢复及相关工具共 9 文件 58 项测试通过；构建、类型检查、相关 ESLint、diff 检查通过。本地 bundle 已升级并重启。真实业务确认变更后直至交付仍待验收。

## 2026-09-12 T03 真实宿主复验

本轮修复 `STALE_VIEW` 后，使用 Codex Chrome 插件在真实 DSH `http://127.0.0.1:3080/` 复验人员管理任务。页面顶部显示当前团队任务，团队进度可展开；需求分析和方案设计保留为“历史审批记录”，功能开发与测试验收显示“已结束”，交付入口显示“查看验收结果”。打开“查看任务资源”后，右侧统一资源面板列出需求规格、需求确认记录、设计方案、架构设计、数据设计、接口契约和验收方案；验收方案正文可见 AC-001–AC-011 与对应证据绑定。整个操作只读，没有发送消息、批准交付或执行数据库迁移。

最终验证报告：`.backend-team/final-verification-cqAQHW/report.json`，状态 `passed`，交付 `ready`，AC-001–AC-011 全部通过且无 unresolved items。截图在本轮 Chrome 验收中直接检查；报告范围仍只覆盖计划内 Node 测试、产物凭据检查及已配置宿主证据，不替代类型检查、通用构建、部署或外部服务验收。当前没有剩余 P0/P1/P2 的 T03 面板显示问题；DELIVER 结果为只读展示，数据库迁移应用仍按独立审批门禁处理。

## 2026-09-13 T04 审批来源与记录复验

使用 Codex Chrome 插件在真实 DSH `http://127.0.0.1:3080/` 打开“团队任务 → 开发一个人员管理功能 → 需求确认记录 → 审批记录”。资源面板保持与聊天并列，审批记录默认折叠，展开后显示两条历史记录及每份文档的哈希摘要；历史记录没有保存认证会话和任务来源，因此页面明确显示“来源未知（历史记录未保存来源）”，没有补造来源信息。当前工作区没有待审批的新记录，故本轮只完成历史记录展示复验，未把历史记录冒充新审批。

本次操作为只读：未发送聊天消息、未接回或搁置任务、未批准审批、未执行数据库迁移。T04 的后端持久化和生产命令来源绑定已有隔离回归；新的真实审批页面仍需下一次业务任务进入 `AWAIT_REQUIREMENTS_APPROVAL` 或 `AWAIT_DESIGN_APPROVAL` 后复验。

同次复验先打开“团队任务”总览：真实列表同时显示已搁置的 greeting 任务、已交付且属于当前对话的人员管理任务、已搁置的客户管理任务以及早期未完成记录；查看列表和资源没有触发执行或改变任务归属。进入人员管理任务资源后再打开需求确认记录，确认“审批记录”默认折叠，展开后两条历史记录均显示“来源未知（历史记录未保存来源）”及对应文档哈希摘要。该状态证明旧任务可查、历史审批不补造来源，但不替代一次带真实会话/任务来源的新审批验收。

## 2026-09-13 T04 持久审批恢复与逐题确认

- 环境：Codex Chrome 插件，真实 DSH `http://127.0.0.1:3080/`，本地 `web` Profile，同一 `travel` 工作区；客户管理任务 `9dca7f07-d6da-4096-b41a-ecf4cbc51883` 处于 `AWAIT_REQUIREMENTS_APPROVAL`、revision 9。
- 问题：重启后 durable phase 能显示“需求待确认”，但审批端的 pending request 只存在内存，页面没有可操作的 DSH 用户确认入口；桥接还曾以未绑定方式调用 `userQuestions.ask`，导致 provider 上下文丢失。
- 处理：生产宿主仅在注入了 DSH `userQuestions` 时按 durable phase 重建内部审批；会话任务宿主把需求/设计审批桥接到 DSH 逐题问题，保留可选意见、版本校验和用户最终批准；关闭时吞掉预期的中止拒绝，避免重启产生未处理异常。
- 复验：升级 Bundle、重启本地 DSH 并刷新同一 Chrome 页面后，聊天真实显示“需求确认 1 / 3”、Q1 问题、选项、“补充说明（选填）”及“下一题”；回答提示“不会自动批准方案”，`确认通过`按钮未出现。截图已在真实宿主中检查，确认卡位于聊天主栏，未遮挡会话列表；资源面板仍可独立查看文档。
- 检查：Node 24 下 `conversation-task-host.test.ts` 3/3 通过；审批来源/生产命令/视图模型及面板模型回归 33 项已通过；Bundle 构建、打包、Profile 升级和 `git diff --check` 通过。未代用户答题、批准需求或执行数据库迁移。
- 剩余差异：当前仍停在需求 Q1，需用户完成 Q1–Q3 并明确批准后，才能验收设计审批及新审批来源记录；深色、窄屏和完整业务链路沿既有 T18/T19/T06 状态处理。

## 2026-09-13 T04 需求重审推荐项提交

- 环境：同一真实 DSH Chrome 宿主 `http://127.0.0.1:3080/`，客户管理任务 `9dca7f07-d6da-4096-b41a-ecf4cbc51883`。
- 用户授权由助手代选需求澄清项后，确认面板逐题提交了推荐选项：Q1 沿用最小字段集合、Q2 联系电话唯一、Q3 授权人员均可修改。每题均在真实确认卡中完成，提交后系统进入需求重新整理并把结论写回需求文档。
- 复验结果：页面随后显示需求规格摘要及“需求审批 / 查看并确认”入口；摘要列出必填名称与联系电话、电话归一化唯一、授权人员可修改、历史审批和旧基线保留。资源区与聊天并列，未出现居中遮罩或输入区遮挡。
- 边界：本轮只代选 Q1–Q3，不代用户点击最终需求批准；设计重审、新审批来源记录及迁移执行均等待需求审批完成后再验收。持久状态为 `AWAIT_REQUIREMENTS_APPROVAL`、revision 11；当前审批卡仍在真实页面可见。

## 2026-09-13 T04 自动验收窄屏审批卡修复

- 真实环境：Codex Chrome 插件，DSH `http://127.0.0.1:3080/?fresh=20260913`，客户管理任务仍停在需求审批确认入口；浏览器显式视口 319×881，深色宿主主题。
- 首次检查发现真实窄屏回归：审批卡宽 223px，但图标、文案和按钮同一 flex 行；文案区只有 23.59px，高度 522.95px，标题与说明被压成逐字竖排。页面没有横向滚动，但确认卡不可读。
- 修复：`packages/web/src/ui/theme.css` 增加 `@media (max-width:399px)`，让图标与文案占首行，确认按钮独占第二行并铺满卡片；保留桌面端单行布局。
- 发布与复验：重建工作区、重新打包并升级本地 `web` Profile，重启 3080；使用新 Tab 和同一 319×881 视口复验。审批卡宽 222.63px，文案宽 138.63px，按钮宽 180.63px，`flex-wrap=wrap`，`scrollWidth=319`，标题和说明恢复正常横向换行，按钮可见且不再挤压文案。已检查窄屏截图，聊天内容、审批入口和输入区均可达。
- 相关检查：8 个 Bundle/Web 回归文件 23/23 通过（含审批卡、逐题确认、任务资源和会话任务宿主）；Web 类型检查、Bundle 构建、打包、Profile 升级和 `git diff --check` 通过。该轮没有点击“查看并确认”、没有批准真实需求、没有执行迁移；T04 仍等待用户批准，完整深色/窄屏业务矩阵仍按 T18 追踪。

## 2026-09-14 T17 图片文件选择入口（代码记录）

本轮按 rc.6 的正式插槽契约新增 `conversation.input.left` 的“添加图片”按钮，使用局部 shadcn ghost/icon 样式。入口只负责选择文件并调用宿主 `conversation.createDraftImages()` 与当前会话 `inputActions.addImages()`；官方 InputBar 原有的粘贴、拖入、预览、取消和图片请求序列不变。会话拒绝时释放已创建的浏览器临时附件，失败文案在入口旁以告警形式呈现。

隔离回归已覆盖成功选择和拒绝释放（2/2）；Web/Bundle 类型检查、构建、相关 ESLint 和 diff 空白检查通过。尚未在正式 DSH Chrome 中实际点击文件选择、拖入并核对模型请求，因此不把 T17 的真实入口和图片传输验收标为通过；截图和正式宿主状态继续由 T17/T19 记录。

## 2026-09-14 T17 真实宿主入口复验

- 环境：Codex Chrome 插件，`http://127.0.0.1:3080/`，本地 `web` Profile，桌面视口；先重启宿主使 Profile 加载本轮 Bundle。
- 结果：输入区真实显示“添加图片”。使用仓库内非敏感 `.backend-team/artifacts/ui-fidelity/host-narrow.png` 通过浏览器文件选择器载入后，输入区出现文件名、缩略图及“移除图片”按钮；DOM 同时暴露“待发送图片”分组，说明附件已进入宿主草稿状态。点击移除后草稿恢复为空。
- 操作边界：本轮未发送消息、未调用模型、未改变任务状态；未把入口选择证据扩展为拖入或模型传输通过。拖入、图片实际进入模型请求仍待 T17/T19 的后续真实验收。

## 2026-09-14 T17 真实宿主拖入复验

- 环境：同一 Codex Chrome 标签页 `http://127.0.0.1:3080/`，桌面视口；输入区已切换到“对话”。
- 操作：将当前对话中已经显示的 `compare-review.png` 图片拖入底部消息输入区。拖入后 DOM 出现“待发送图片”分组、缩略图和“移除图片”按钮，发送按钮从禁用变为可用；证据 `.backend-team/artifacts/image-drag-chrome-evidence-20260914.json`。
- 清理：点击“移除图片”，草稿恢复为空；未发送消息、未调用模型、未改变团队任务。
- 限制：本次证明真实宿主的拖入事件和草稿接入链路；拖入本地文件系统的动作无法通过当前浏览器自动化接口直接模拟，正式图片模型请求已有独立证据，T17/T19 仍需合并矩阵核对后再勾选。

## 2026-09-14 T17 隔离图片模型请求复验

- 环境：Codex Chrome 插件，真实 DSH `http://127.0.0.1:3080/`，新建空会话，Qwen3.8 Flash，使用仓库内 `compare-review.png`。
- 结果：通过“添加图片”选择有效 PNG 后发送短请求，页面显示图片缩略图、进行中状态及最终中文描述；单轮请求完成并显示耗时与 token 统计，当前会话未关联团队任务。结构化证据见 `.backend-team/artifacts/image-model-chrome-evidence-20260914.json`。
- 夹具修复：首次使用的 `host-narrow.png` 内容实际为 JPEG，宿主在发送阶段按支持格式拒绝；已将该文件转换为真实 PNG（768×900），避免后续测试因扩展名/MIME 不一致失败。
- 操作边界：本轮没有发送业务需求、没有调用团队工作流、没有修改客户样例或业务文件；正式宿主拖入动作仍未取得证据。

## 2026-09-14 T18 窄屏顶部布局复验

- 环境：Codex Chrome 插件，真实 DSH `http://127.0.0.1:3080/`，390×844 视口；当前为隔离图片验收会话。
- 首次差异：宿主标题栏中的模式、当前任务和右上操作区在窄屏发生挤压；注入的两个长文本按钮占用宽度后，顶部出现重叠。
- 处理：仅在 640px 以下将本插件注入的“当前团队任务”和“团队任务”按钮收为图标按钮，保留 `title`/无障碍名称；桌面端文字按钮不变。
- 复验：顶部标题、模式和操作区不再重叠；`document.documentElement.scrollWidth`、`document.body.scrollWidth` 与视口均为 390。恢复默认视口后桌面端仍显示完整文字按钮。完整深色、资源面板和审批场景仍按 T18/T19 继续验收。

## 2026-09-14 T18 深色资源与审批记录复验

- 环境：Codex Chrome 插件，真实 DSH `http://127.0.0.1:3080/`，桌面视口；通过宿主设置切换“深色”。
- 结果：聊天、左侧会话列表、顶部团队任务入口、底部输入区和右侧“任务资源”均保持可读；打开“需求规格”后，资源预览、相关文档、版本信息和审批记录仍在右侧并列，不遮挡聊天。展开审批记录后，需求/设计两条记录均显示“来源已记录”、同一认证会话和任务 ID 以及文档摘要；当前仅查看，没有触发审批或业务写入。证据 `.backend-team/artifacts/t18-dark-resource-chrome-evidence-20260914.json`。
- 检查：`body[data-ds-dark-theme]`、`color-scheme: dark`，`document/body.scrollWidth=1920`，composer 仍可见。窄屏 390px 的浅色/布局证据沿用本节上一条记录；当前浏览器扩展不接受显式 viewport 覆盖，未把未生效的覆盖误报为深色窄屏通过。

## 2026-09-15 T18 深色窄屏审批卡复验

- 首次复现：在隔离正式宿主 `http://127.0.0.1:3095/` 的 390×844 视口中，`bt-progress-summary-row` 固定三列在可用宽度不足时把“正在做”说明挤成逐字换行，当前进展卡高度异常；这是团队主题自身的窄屏网格约束，不是用户业务内容问题。
- 修复：`packages/web/src/ui/theme.css` 在 760px 以下改为图标列 + 单一内容列，标签与值纵向排列，并保留可读行高；桌面三列布局不变。
- 复验：重新构建并升级同一临时 `web` Profile 后，深色窄屏下四个摘要行网格为 `24px 168px`，高度分别为 145/82/82/103px，说明文字恢复正常横向换行；审批卡显示 `migration.sql`、完整 SQL、“退回修改”和“确认并继续”，聊天输入区仍可见，`document/body.scrollWidth=390`。未点击确认或退回。
- 证据：`.backend-team/artifacts/t18-dark-narrow-approval-chrome-20260915.json`；截图由 Codex Chrome CUA 直接检查。仅使用临时 Profile/工作区/PostgreSQL，未执行迁移或生产写入。

## 2026-09-14 T04 任务中心与历史审批来源复验

- 环境：Codex Chrome 插件，真实 DSH `http://127.0.0.1:3080/`，本地 `web` Profile；隔离图片会话打开“团队任务”。
- 结果：任务中心列出工作区全部任务，包含已搁置、已完成、未完成及早期记录；进入已完成的人员管理任务后，资源区可查看需求确认记录和文档。
- 审批记录：展开“审批记录”后，两条历史记录均显示“来源未知（历史记录未保存来源）”及文档哈希摘要，符合不补造历史来源的约束。
- 操作边界：本轮仅查看任务和资源，未接回、搁置、批准、发送业务需求或执行数据库操作。当前没有新的待审批记录，真实会话/任务来源链路仍待下一次审批产生后复验。

## 2026-09-14 T19 Chrome 全流程矩阵核对

- 环境：Codex Chrome 插件，真实 DSH `http://127.0.0.1:3080/`，本地 `web` Profile，`travel` 工作区；当前会话为隔离图片验收会话，页头仍显示客户管理团队任务关联。
- 操作：打开“团队任务”后核对工作区任务列表。真实列表同时显示已搁置 greeting、已完成人员管理、未完成客户管理和早期工作区记录；客户管理项带“当前对话的任务”标记。搜索框、全部/未完成/已搁置/已完成筛选和关闭入口可见，聊天主栏与任务中心并列，未遮挡 composer。
- 结果：该行“打开 Backend Team panel”记为真实宿主只读通过。需求/设计审批、Agent 活动、运行时安装、DbGate→迁移、恢复和卸载按已有证据分别记为部分、通过或阻塞，完整矩阵写入 `.backend-team/artifacts/browser-acceptance-matrix-t19-20260914.json`。
- 边界：本轮没有发送业务需求、接回/搁置任务、批准需求/设计、执行迁移或卸载 Profile；任务中心列表可见不等于同一生产组合下的十个场景全部通过。T19 因 Harness provenance、双架构发布、正式迁移接线和干净 Profile 生命周期缺口保持未完成。

## 2026-09-14 T19 隔离 E2E 矩阵复跑

- 环境：工作区 Node 24、现有 E2E 合同测试；不连接业务数据库、不发送聊天消息、不调用真实模型。
- 结果：使用工作区 `.backend-team/runtime` Node 24.19.0 运行 E2E 矩阵，7 个文件、16/16 测试通过，耗时 1.26s；结果见 `.backend-team/artifacts/browser-acceptance-matrix-t19-20260914-rerun.json`。
- 说明：该轮验证场景契约、阶段断言和清理断言，不能替代同一生产组合的 Codex Chrome 十场景流程。真实宿主只读任务中心证据仍沿用上一节，T19 继续保持 `partial`。

## 2026-09-14 T19 3080 任务中心只读复验

- 环境：Codex Chrome 插件，真实 DSH `http://127.0.0.1:3080/`，`travel` 工作区，Tab `154941152`；未改变用户会话内容。
- 结果：打开“团队任务”后，AX/DOM 同时显示已搁置的 greeting、已完成的人员管理、当前对话的客户管理和早期工作区任务；搜索框、全部/未完成/已搁置/已完成筛选、刷新和关闭入口均可见。聊天主栏与右侧团队任务资源并列，composer 仍可见，未出现遮挡或横向滚动。
- 操作边界：本轮只打开任务中心并读取 DOM/截图，未发送消息、接回/搁置任务、批准需求/设计、执行迁移或卸载 Profile。截图已由 Codex Chrome CUA 检查；当前接口不提供把截图字节写回工作区的能力，因此以 Tab、URL、AX/DOM 文本和机器证据文件作为可复核记录。

## 2026-09-14 T15 数据库备份资源入口复验

- 环境：Codex Chrome 插件，真实 DSH `http://127.0.0.1:3080/`，本地 `web` Profile，桌面视口；当前页为图片链路验收会话，页头仍显示客户管理团队任务关联。
- 首次问题：快照清单读取异常会让资源接口整体返回 500，导致需求/设计文档也无法查看。处理后，快照清单失败降级为空列表，文档资源继续可读，不再把数据库状态异常扩散成整个资源面板不可用。
- 复验：重启正式 DSH 后刷新同一 Chrome 标签页，任务中心仍列出工作区全部 4 个任务并标出当前任务；打开当前任务的“数据设计”后，右侧资源预览中可见“数据库备份”区、“暂无备份”、“备份原因”输入框和“立即备份”入口。聊天与资源面板并列，页面无居中遮罩；本轮通过 Codex Chrome 截图和 AX 树检查，未点击写入操作。
- 证据：`.backend-team/artifacts/database-snapshot-resource-chrome-evidence-20260914.json`。本轮截图由 Codex Chrome CUA 直接捕获并检查；该接口不提供将截图字节写入工作区的能力，因此保留可复核的标签、任务、URL、Tab ID 与操作边界记录。
- 检查：Node 24 下快照/资源/控制相关回归 4 个文件 11 项通过；迁移恢复、宿主恢复和 PostgreSQL 集群回归 3 个文件 25 项通过；Bundle/Web 类型检查、构建、Profile 升级和正式 DSH 重启均通过。
- 限制：当前真实客户管理任务属于用户业务任务，且不存在可安全写入的隔离快照；没有代用户点击“立即备份”或“确认恢复”。T15 的正式宿主实际备份/恢复结果仍待隔离任务证据，T14 的重启确认卡与失败恢复页面也仍待真实宿主证据。

## 2026-09-14 T14 隔离恢复失败页面复验

- 环境：Codex Chrome 插件，真实 DSH `http://127.0.0.1:3092/`，临时 DSH_HOME/Profile/工作区；任务 `T14 隔离迁移恢复演练`，桌面视口。
- 根因与修复：共享 `pending-migration.json` 会被旧任务宿主先读取，造成新任务看不到自己的待审批记录。持久化存储现按任务 ID 使用 `pending-migration-<taskId>.json`，无任务调用仍使用 legacy 文件，避免跨任务消费并保留兼容。
- 复验结果：重启后页面保持可访问，任务头部显示“团队任务 · 需求分析 已受阻”；当前进展、当前问题和下一步显示“待审批迁移记录无效，已阻止恢复”，点击“查看技术详情”后显示 JSON 解析错误及轨迹提示。页面未显示迁移成功或批准状态，`sqlApplied=false`。
- 证据：`.backend-team/artifacts/migration-recovery-failure-chrome-t14-20260914.json`，包含标签页、任务 ID、错误文本、技术详情可见性和操作边界。截图由 Codex Chrome CUA 捕获并检查；该接口不提供将截图字节写回工作区的能力，因此保留可复核的 URL、Tab ID、AX 文本和 JSON 摘要。
- 限制：仅为隔离损坏记录演练，未连接客户业务任务，未执行用户审批、数据库迁移或生产写入；有效待审批重启确认卡、过期/篡改记录的正式宿主证据仍待补。

## 2026-09-14 T14 有效确认卡与失效记录复验

- 环境：Codex Chrome 插件，临时 DSH `http://127.0.0.1:3093/`，桌面视口；临时 Profile `/tmp/dsh-t14h-6jpOY1`、工作区 `/private/tmp/dsh-t14v-M6nX3X`，任务 `T14 隔离迁移恢复演练`。
- 根因修复：任务进度页此前只调用 `open-artifact`，没有把返回的预览写回 `artifactPreview`，用户点击后看不到 SQL。现在回调校验返回哈希并更新页面；预览出现后才开放“退回修改/确认并继续”。
- 有效记录复验：重启后页面显示“等待你处理 / 需要确认 / 数据库迁移”；点击“查看方案”后显示 `migration.sql` 和完整 `CREATE TABLE "accounts"` SQL，同时显示“退回修改/确认并继续”。未点击任何决定，`sqlApplied=false`。证据：`.backend-team/artifacts/migration-review-restart-confirmation-chrome-t14-20260914.json`。
- 过期记录复验：将待审批记录 revision 设为 1、宿主状态 revision 为 2；重启后任务显示“已受阻”，页面明确显示“迁移审批已过期，请重新生成迁移”，待审批文件被清理。证据：`.backend-team/artifacts/migration-review-expired-chrome-t14-20260914.json`。
- 篡改记录复验：保持 SQL 摘要不变而篡改请求中的迁移摘要；重启后页面显示 `pending migration approval hash does not match preview`，技术详情可展开，待审批文件被清理。证据：`.backend-team/artifacts/migration-review-tampered-chrome-t14-20260914.json`。
- 验收边界：以上均为隔离临时数据，未连接客户业务任务，未批准审批、执行迁移或写入生产库；截图由 Codex Chrome CUA 直接检查，截图字节无法通过当前接口写回工作区。

## 2026-09-14 T15 资源面板备份/恢复结果复验

- 环境：Codex Chrome 插件，临时 DSH `http://127.0.0.1:3093/`，临时 Profile `/tmp/dsh-t14h-6jpOY1`、工作区 `/private/tmp/dsh-t14v-M6nX3X`，任务 `T14 隔离迁移恢复演练`；使用隔离 PostgreSQL 18.6 arm64 实例。
- 交互修复：资源面板从资源响应中同步当前任务 ID。此前从进度页打开资源时任务上下文为空，备份/恢复入口虽然显示但不会执行；修复后两项操作都能发起，并在面板内保留结果状态。
- 备份复验：在右侧“任务资源 → 数据库备份”输入原因 `T15 GUI 结果展示验收`，点击“立即备份”，面板显示绿色状态“数据库备份已完成，可在下方查看校验摘要。”，并列出 3 份备份；最新快照为 `20260914T130653Z-postgres-95fe05fc`。
- 恢复复验：预先创建空库 `restored_gui2`，点击最新快照“恢复到新库”，填写目标库并确认；面板显示绿色状态“数据库已恢复到新库：restored_gui2”。独立查询回读源数据 `1|t15-ui`，恢复库同样为 `1|t15-ui`。
- 视觉/可用性：聊天与资源面板仍并列，结果状态位于数据库备份区顶部，按钮操作后有明确反馈；未出现居中遮罩或聊天内容遮挡。截图由 Codex Chrome CUA 直接捕获并检查。
- 证据：`.backend-team/artifacts/database-snapshot-resource-chrome-t15-20260914.json`（权限 600）。
- 边界：仅为临时 Profile、工作区和数据库的隔离操作；未连接客户业务任务，未执行正式审批、迁移或生产数据库写入。T15 的正式客户任务边界仍需后续单独验收。

## 2026-09-15 T13 同会话 DbGate→迁移验收

- 环境：Codex Chrome 插件，隔离 DSH `web` Profile `http://127.0.0.1:3095/`，任务 `T14 隔离迁移恢复演练`；同一会话打开 DbGate 7.2.3 `http://127.0.0.1:3094/`。
- GUI 操作：在 `design_fd7eece23e1be9cf` 的 public schema 中创建 `gui_migration_accounts`，保留 `id` 主键并增加 `email text`；DbGate 显示“Saved to database”。
- 接线与预览：返回同一 DSH 会话后调用数据库迁移准备，页面显示“需要确认 / 数据库迁移”和完整 `migration.sql` 预览；预览包含 `CREATE TABLE "gui_migration_accounts"` 与 `email` 字段。页面截图及 AX 树由 Codex Chrome CUA 直接检查。
- 批准后结果：在临时数据库范围内确认并继续，页面审批卡消失，状态回报“数据库迁移已应用，备份与迁移文件已保存。”；ORM `schema.ts` 已写入，独立 PostgreSQL 查询回读 `true|id,email`。证据 `.backend-team/artifacts/formal-host-t13-same-session-20260915.json`。
- 边界：仅使用临时 Profile、工作区和 PostgreSQL 集群；未修改正式业务工作区或业务数据库。该轮证明同一宿主会话的 GUI 设计、迁移预览、ORM 落盘和数据库回读链路。

## 2026-09-15 T14 正式宿主重启恢复

- 环境：Codex Chrome 插件，隔离 DSH `web` Profile，宿主 `http://127.0.0.1:3095/`，DbGate 7.2.3 `http://127.0.0.1:3094/`；任务 `T14 隔离迁移恢复演练`，任务 ID `2de6b4a6-0d3e-4d99-a0dd-4f6db8366e5a`。
- 前置 GUI 操作：同一宿主会话打开 DbGate，在设计库 `design_09f673bb26015db2` 通过 SQL 编辑器创建 `public.t14_restart_recovery (id integer PRIMARY KEY, note text NOT NULL)`；DbGate 显示执行完成。返回宿主生成迁移后，页面显示“需要确认 / 数据库迁移”，点击“查看方案”可见完整 `migration.sql` 与“退回修改/确认并继续”。未点任何决定。
- 重启操作：停止隔离宿主及其临时 PostgreSQL，再用同一 Profile、工作区和任务重新启动。旧设计会话记录保存的 PostgreSQL 端口为 `56273`，重启后实际端口为 `53510`；宿主状态保持 `stateRevision=4`，待审批 ID `migration-52f9cacf-de4d-4122-a351-aae147ae5e20`、迁移摘要 `0bc6fd8ad7ce8fd4cd12b0ec84687617b24b9c5cc9c75f9cb266774e665d3622` 均恢复一致。
- 重启后 GUI：Chrome 重新打开宿主后仍显示“等待你处理 / 需要确认 / 数据库迁移”；点击“查看方案”再次显示相同 `CREATE TABLE "t14_restart_recovery"` SQL 和两个决定按钮。未代用户审批，页面没有自动执行迁移。
- 数据库证据：重启后独立 PostgreSQL 查询显示 sourceDatabase 中该表计数为 `0`、designDatabase 中计数为 `1`，`sqlApplied=false`；待审批文件仍保留。该结果证明端口变化不会误阻止合法恢复，同时旧批准没有绕过当前 SQL 校验。
- 证据文件：`.backend-team/artifacts/migration-review-restart-confirmation-formal-host-t14-20260915.json`。截图和 AX 树由 Codex Chrome CUA 直接检查；当前接口不能把截图字节写回工作区，因此记录标签、URL、任务、可见文案和机器查询结果供复核。
- 检查：Bundle 类型检查通过；迁移审批、原生 PostgreSQL、宿主恢复、DbGate 启动及安全回归共 8 个测试文件 `58 passed | 2 skipped`；`git diff --check` 和证据 JSON 解析通过。
- 边界：仅使用临时 Profile、工作区和 PostgreSQL 集群；未批准迁移、未将 SQL 应用到 sourceDatabase、未触碰正式业务任务。损坏、过期和摘要篡改的诊断路径沿用既有 T14 证据。

## 2026-09-16 T05 团队执行阻塞与恢复入口复验

- 环境：Codex Chrome 插件，真实本地 DSH `http://127.0.0.1:3080/`，`travel` 工作区；页面切换到“团队进度”页签，未启动已有用户任务。
- 首次问题：旧 Bundle 在调度预算超限后只留下事件错误，重启后页面回到“尚未运行”；输入区下方的团队进度卡也只有“暂停任务”，受阻后没有可执行的恢复入口。
- 修复：协调器和宿主持久化并恢复终态失败；显式恢复前清理旧错误；进度卡在 BUILD/VERIFY 的 `blocked`、`failed`、`paused`、`idle` 状态显示“恢复任务”，并保持暂停/恢复互斥。
- 复验：3080 重启加载新 Bundle 后，团队进度页显示“功能开发 · 已受阻”、完整预算原因 `tokens used 1379962 > limit 1000000`、当前问题和下一步，并显示“恢复任务”按钮；“当前进展”改为“任务已受阻，处理原因后可恢复”，避免把失败原因误放在“正在做”项下；聊天输入区仍可见，未出现遮挡。隔离宿主已实际点击恢复并进入运行态；正式 3080 本轮未点击恢复，避免启动已有长任务。
- 证据：`.backend-team/artifacts/local-acceptance-agent-team-20260916.json`；页面状态由 Codex Chrome CUA 的 AX/DOM 快照确认。
