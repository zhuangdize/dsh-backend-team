# Specification Agent integration

User-approved direction: continue direct model API integration and complete the requirements/design workflow without granting pre-approval business-code access.

- [x] Dispatch structured expert AgentTask contracts, matching AgentResult IDs and bounded usage.
- [x] Prefix artifacts with the active `specs/<feature>` directory.
- [x] Add separate specification create/update policy APIs, retaining BUILD/VERIFY policy boundaries.
- [x] Install document-only tools with identity, exact paths, host policy, approval and durable ownership checks.
- [x] Tie document ownership cleanup to the actual Cordis Agent lifecycle.
- [x] Verify image clipboard preview, submission and actual Qwen vision response through the Codex Chrome extension.
- [x] Complete combined regression, typecheck and independent review.
- [x] Run bounded real Qwen SPECIFY production activation probe and record its limits.

- [x] Extend isolated real-model checks to DESIGN and PLAN with explicitly seeded test approvals.

- [x] Add explicit task token/time ceiling overrides after the real cumulative-budget rejection.
- [x] Add one tool-free result-format correction, preserve strict semantic checks, and await shared disposal during cancellation.

The ordinary DSH Web profile remains diagnostic for BackendTeam production actions. A successful isolated probe does not establish end-to-end production UI approval, design, development or delivery acceptance.

Final evidence: 60 related test files / 602 passed, workspace typecheck and
touched-file lint, build and packed checks passed. Separate real SPECIFY and
seeded DESIGN/PLAN probes passed their document I/O and result boundaries.
The real successful DESIGN/PLAN run needed no format correction; correction
and cancellation paths are verified by deterministic regressions.
