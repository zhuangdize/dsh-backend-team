# 项目识别与团队上下文

2026-09-09 接入正式 `workflow-host` 的 Spec Kit 命令加载入口。每次需求、澄清、设计、任务规划都会重新运行现有 ProjectAnalyzer，并向专家提供精简的结构化事实：项目类型、技术及证据路径、Node 声明、服务边界、存在的问题、尚未执行的检查候选，以及宿主实际 Node 版本和允许写入的目录。

分析只读取文件，不运行项目脚本、不导入目标应用。提示中不包含脚本正文、环境变量文件或源代码片段。团队内部状态和运行时不参与业务扫描；测试夹具、示例与模板不作为业务框架/数据库证据，但仍保留测试工具识别。

策略选择结果是建议，不是更换框架、迁移数据库或写文件的授权。服务边界未确认时明确保留空值；原审批、权限和开发前基线检查继续生效。候选检查全部标作未验证，不能把检测到 build/typecheck/lint 当作已经执行。当前通用检查执行、多个服务的完整选择流程及空目录自动初始化仍未完成。

验证：13 个相关测试文件 105 项通过；构建、类型检查、相关 ESLint 通过。额外覆盖目标项目脚本不会执行、秘密不进入提示、设计阶段重新读取新配置、排除运行时和测试样本。独立目录中加载打包后的生产模块通过，修复了分析器 CommonJS 依赖在 ESM 包内的加载问题，并补充相应许可证。当前工作区进行了真实只读分析，没有生成或批准业务文档。

## 执行计划路径预检（2026-09-10）

正式宿主的 taskPlanLoader 在生成 tasks.md 后、进入 BUILD 前调用只读 validateDevelopmentPaths：核对可写根目录、受控工具不支持的隐藏路径、文件与目录重叠、现有父目录/目标文件类型、符号链接及硬链接。检查整个计划后才允许创建父目录；启动执行时再次检查，并先验证基线边界再创建目录。

路径错误进入现有规划器的一次自动修正流程，只允许修正 tasks.md，不能改变已批准设计或扩张权限；再次失败保留 PLAN 并报告具体路径。注意执行计划在设计批准后生成，本改动不是“设计审批前全部环境已验证”。旧 BUILD 任务也不会自动改写或重新审批。

验证：4 个文件 40 项测试通过（路径检查、规划失败/修正状态、宿主挂载和独立包加载），完整构建、类型检查、相关 ESLint、diff 检查通过。更新已安装至本地 web profile。未运行真实业务开发或代用户审批。依赖安装、通用验证、环境自动恢复仍未完成。

Chrome 插件复查正式 3080 页面：启动前本地端口没有服务；重新启动后原会话恢复，任务标题正确，执行状态“尚未运行”，输入框可见。记录位于 `.backend-team/artifacts/planning-preflight-chrome.txt`。这是启动/连接检查，不代替真实模型修正计划和业务交付验收。
