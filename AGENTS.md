# 项目协作约定

沿用用户的通用 AGENTS.md 指令。UI 变更另须遵守：

- 接到“继续”、实施任务或查询进度时，先读根目录 [TODO.md](TODO.md)，按“下一步”和编号推进；每轮更新任务状态、验证证据及下一步，完成对应标准才勾 ☑️。TODO.md 是当前状态唯一入口；[原需求核查](docs/requirements-audit-2026-09-09.md) 保留为需求来源与历史依据，局部测试不替代业务主流程验收。

- 修改前阅读 [设计规范](docs/design/team-ui.md) 与 [视觉验收记录](design-qa.md)，打开规范中的已确认原稿。
- 团队交互采用 shadcn/ui 组件与统一局部主题；DSH 仍管理会话、权限和审批，不改变后端授权语义。
- 聊天为主，右侧资源审阅并列，不用居中遮罩。问题逐题确认，文档审批支持可选意见。
- 交付前使用 Codex Chrome 插件，在真实 DSH 宿主中与原稿对照；截图、状态和剩余差异写入 design-qa.md。构建与测试通过不能代替视觉通过。
- 不覆盖已有业务文档和未提交修改，不为验收代用户批准真实任务。
- 新增问题或审批类型时，必须用生产端实际的 `id/header/payload` 验证客户端选择器及信息展示；不能只测试工具返回值。真实宿主若回退到默认控件，要修复并重新截图验收，不能称作已复用确认稿组件。
- 范围防跑偏：以 `TODO.md` 顶部“下一步”和“当前任务范围锁”为准。当前工作目标是 Backend Agent Team 本身；`src/customers/` 与 `test/customer-model.test.mjs` 仅是已生成需求的验收样例，除用户明确要求外冻结，不得用样例实现或样例测试替代 Agent Team 任务证据。
